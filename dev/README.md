# Sentinel Dev Workspace

This folder holds early application work before the project structure is finalized.

- `Data Model/`: database model artifacts
- `frontend/`: client-side UI work
- `server/`: backend/server work
- `shared/`: contracts or shared types between frontend and server
