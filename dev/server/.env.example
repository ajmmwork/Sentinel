DATABASE_URL=postgresql+psycopg://username:password@localhost:5432/sentinel

# Optional for local development.
# If SMTP_HOST is not set, Sentinel prints verification codes to the terminal.
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
EMAIL_FALLBACK_TO_CONSOLE=true
